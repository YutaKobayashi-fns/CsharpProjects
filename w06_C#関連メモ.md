# **C#に関するメモ  **  

作成者：小林 祐太  
___  
___  

### w06_01　「C#基礎」  

* [エントリーポイント](https://ufcpp.net/study/csharp/misc/miscentrypoint/)：  
  実行可能なプログラムのうち、最初に呼び出される処理のこと。  
  基本的にはCやC++と同様に「Main」メソッド（静的）がエントリーポイントとなるが、  
  C＃9.0からはトップレベルステートメントという書き方のエントリーポイントも使用可能。  
  ※ エントリポイントは1プロジェクトで1つしか設定できない。(コンパイルでワーニングが出る)   
  　例として、Mainトップレベルステートメントを含むプロジェクトでMainメソッドを追加しても  
　メインメソッドの処理は実行されず、無視されてしまう。  
  ___  

* [トップレベルステートメント(最上位レベルのステートメント)](https://learn.microsoft.com/ja-jp/dotnet/csharp/fundamentals/program-structure/top-level-statements)：  
  基本構造は「Main」メソッドを準備してから処理をメソッド内に追加して行くが、  
  トップレベルステートメントの思想でコーディングをする場合、Mainメソッドは不要で  
  ファイルのルート（using等の宣言後）に直接記載することで処理を実行することができる。  
  また、トップレベルステートメントはグローバル名前空間に属する。  
  （使用用途としては、小規模なユーティリティツールの作成等を作成するのに有効）  
  ___  

* [ユージングディレクティブ](https://saas.n-works.link/programming/c-sharp/how-to-c-using#:~:text=Using%E3%83%87%E3%82%A3%E3%83%AC%E3%82%AF%E3%83%86%E3%82%A3%E3%83%96%E3%81%AF%E3%80%81%E5%90%8D%E5%89%8D%E7%A9%BA%E9%96%93,%E6%8C%87%E5%AE%9A%E3%81%99%E3%82%8B%E5%BF%85%E8%A6%81%E3%81%8C%E3%81%82%E3%82%8A%E3%81%BE%E3%81%99%E3%80%82)：  
  名前空間内に定義したクラスを、※完全修飾名 なしで使用するための機能。  
  イメージとしては、C言語で言うところの「ヘッダのインクルード」に近い。  
※ 完全修飾名⇒例として、System名前空間内のText名前空間にある、Encodingクラスを使用したい場合、  
　System(名前空間の).Text(名前空間にある).Encoding(クラス) の様に記述する。  
  ___  

* [nullable(null許容値型)](https://ufcpp.net/study/csharp/sp2_nullable.html)：  
  通常、値型は入力された値がnullの場合にnull値を取得できない。  
  しかし、null許容型の場合は入力値がnullであってもnull値を含めて値の取得ができる。  
  ___  

* [タプル型]()：  
  異なる値（型も含む）をまとめることができる、簡易的な構造体のようなもの。  
  ___  

* var（型推論）：  
  初期値から変数の型が変わるような場合に使われる。  
  代入値に合わせて型を可変してくれる。  
  varで宣言されたローカル変数は「暗黙的型指定ローカル変数」と呼ばれる。  
  また、varで宣言されたローカル変数は、必ず何らかの形で初期化が必要となる。  
  ___  

* インクリメント・デクリメントに関すること：  
  一般的に利用機会の多い「val++、val--」をコマンドライン出力の引数で使用すると
  ①文字列操作で現在の値を出力⇒②インクリメント といった順番で処理される。  
  対して、「++val、--val」をコマンドライン出力の引数で利用すると  
  ①現在の値をインクリメント⇒②文字列操作でインクリメント後の値を出力  
  という様に、処理の順番が逆転する。  
  ___  

* 条件演算子：  
  書式としては「 aaa ? bbb : ccc ;」(?:)。  
  aaa が条件式、bbb がtrueケースの処理、ccc がfalseケースの処理。  
  ___  

* string型のIndexOf(".")で取得できるデータ(例：I like cat. So cute!!)  
  ⇒"."が何文字目にあるかではなく、"."が「文字列のIndex(0based)でみて何番目か？」を取得する。
    上記例の場合、11文字目なのでIndex=10が取得できる。
  ___  

* キャスト と 変換(Covert) の使い分け  
  ⇒ 結果が切り捨てでも可の場合は「キャスト」  
　　 四捨五入(丸め)の結果を期待する場合は「変換(Covert)」  
  ___  

* StringSplitOptions.RemoveEmptyEntries  
  配列から、空の要素（空文字""の要素）を除外できる。  
  使い方例：ip.Split(".", StringSplitOptions.RemoveEmptyEntries);※ipはstring型  

  